<!-- Autogenerated by mlir-tblgen; don't manually edit -->
### `aismem.Add` (::spade::AISMEMAddOp)

_AISMEM Add Operator_

Performs element-wise binary addition (no support for Numpy-style broadcasting support).

Traits: `AlwaysSpeculatableImplTrait`

Interfaces: `ConditionallySpeculatable`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `A` | memref of any type values
| `A_shape` | memref of any type values
| `B` | memref of any type values
| `B_shape` | memref of any type values
| `C` | memref of any type values

#### Results:

| Result | Description |
| :----: | ----------- |
| `none_val` | none type

### `aismem.ConvEx` (::spade::AISMEMConvExOp)

_AISMEM ConvEx operation_

The convolution operator consumes an input tensor and a filter, and
computes the output.
Y= conv(X,W)

Traits: `AlwaysSpeculatableImplTrait`

Interfaces: `ConditionallySpeculatable`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `X` | memref of any type values
| `X_shape` | memref of any type values
| `W` | memref of any type values
| `W_shape` | memref of any type values
| `padding` | memref of any type values
| `stride_dilation` | memref of any type values
| `B` | memref of any type values
| `Y` | memref of any type values

#### Results:

| Result | Description |
| :----: | ----------- |
| `none_val` | none type

### `aismem.Conv` (::spade::AISMEMConvOp)

_AISMEM Conv operation_

The convolution operator consumes an input tensor and a filter, and
computes the output.
Y= conv(X,W)

Traits: `AlwaysSpeculatableImplTrait`

Interfaces: `ConditionallySpeculatable`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `X` | memref of any type values
| `X_shape` | memref of any type values
| `W` | memref of any type values
| `W_shape` | memref of any type values
| `padding` | memref of any type values
| `stride_dilation` | memref of any type values
| `Y` | memref of any type values

#### Results:

| Result | Description |
| :----: | ----------- |
| `none_val` | none type

### `aismem.GEMM` (::spade::AISMEMGEMMOp)

_AISMEM GEMM Operator_

General Matrix multiplication: https://en.wikipedia.org/wiki/Basic_Linear_Algebra_Subprograms#Level_3
with the following hardcoded attributes
alpha = 1.0, betha = 1.0
transA = transB = 0 , i.e. no transpose

Traits: `AlwaysSpeculatableImplTrait`

Interfaces: `ConditionallySpeculatable`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>transA</code></td><td>::mlir::IntegerAttr</td><td>64-bit signed integer attribute</td></tr>
<tr><td><code>transB</code></td><td>::mlir::IntegerAttr</td><td>64-bit signed integer attribute</td></tr>
</table>

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `A` | memref of any type values
| `A_shape` | memref of any type values
| `B` | memref of any type values
| `B_shape` | memref of any type values
| `C` | memref of any type values
| `Y` | memref of any type values

#### Results:

| Result | Description |
| :----: | ----------- |
| `none_val` | none type

### `aismem.MatMul` (::spade::AISMEMMatMulOp)

_AISMEM MatMul Operator_

Matrix product that behaves like numpy.matmul: https://docs.scipy.org/doc/numpy-1.13.0/reference/generated/numpy.matmul.html

Traits: `AlwaysSpeculatableImplTrait`

Interfaces: `ConditionallySpeculatable`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `A` | memref of any type values
| `A_shape` | memref of any type values
| `B` | memref of any type values
| `B_shape` | memref of any type values
| `C` | memref of any type values

#### Results:

| Result | Description |
| :----: | ----------- |
| `none_val` | none type

### `aismem.MaxPool` (::spade::AISMEMMaxPoolOp)

_AISMEM MaxPool operation_

Max pooling consisting of computing the max on all values of a subset of the input tensor according
to the kernel size and downsampling the data into the output tensor Y for further processing.

Traits: `AlwaysSpeculatableImplTrait`

Interfaces: `ConditionallySpeculatable`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `X` | memref of any type values
| `X_shape` | memref of any type values
| `KernelStrides` | memref of any type values
| `Padding` | memref of any type values
| `Y` | memref of any type values

#### Results:

| Result | Description |
| :----: | ----------- |
| `none_val` | none type

### `aismem.qconstant` (::spade::AISMEMQConstantOp)

_AISMEM quad-word constant operation_

Operation for holding constant data values. A quadword constant can have a
meaningful name recorded as its `name` attribute. Its content is stored
in the `value` dense/opaque element attribute.

Traits: `AlwaysSpeculatableImplTrait`, `MemRefsNormalizable`

Interfaces: `ConditionallySpeculatable`, `NoMemoryEffect (MemoryEffectOpInterface)`, `mlir::ResultTypeInferenceOpInterface`

Effects: `MemoryEffects::Effect{}`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>shape</code></td><td>::mlir::Attribute</td><td>any attribute</td></tr>
<tr><td><code>name</code></td><td>::mlir::StringAttr</td><td>string attribute</td></tr>
<tr><td><code>value</code></td><td>::mlir::Attribute</td><td>any attribute</td></tr>
</table>

#### Results:

| Result | Description |
| :----: | ----------- |
| `output` | memref of any type values

### `aismem.ReduceMean` (::spade::AISMEMReduceMeanOp)

_AISMEM ReduceMean Operator_

Computes the mean of the input tensor's element along the provided axes.

Traits: `AlwaysSpeculatableImplTrait`

Interfaces: `ConditionallySpeculatable`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `X` | memref of any type values
| `X_shape` | memref of any type values
| `Axes` | memref of any type values
| `Y` | memref of any type values

#### Results:

| Result | Description |
| :----: | ----------- |
| `none_val` | none type

### `aismem.Relu` (::spade::AISMEMReluOp)

_AISMEM Relu operation_

 rectified linear function
Relu(input,io_shape,output)

Traits: `AlwaysSpeculatableImplTrait`

Interfaces: `ConditionallySpeculatable`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `X` | memref of any type values
| `X_shape` | memref of any type values
| `Y` | memref of any type values

#### Results:

| Result | Description |
| :----: | ----------- |
| `none_val` | none type

### `aismem.Reshape` (::spade::AISMEMReshapeOp)

_AISMEM Reshape Operator_

Reshape the input tensor similar to numpy.reshape. 
First input is the data tensor, second input is a shape tensor which specifies the output shape. 
It outputs the reshaped tensor.

Traits: `AlwaysSpeculatableImplTrait`

Interfaces: `ConditionallySpeculatable`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `X` | memref of any type values
| `X_shape` | memref of any type values
| `Y_shape` | memref of any type values
| `Y` | memref of any type values

#### Results:

| Result | Description |
| :----: | ----------- |
| `none_val` | none type

### `aismem.Softmax` (::spade::AISMEMSoftmaxOp)

_AISMEM Softmax Operator_

The operator computes the normalized exponential values for the given input:

Traits: `AlwaysSpeculatableImplTrait`

Interfaces: `ConditionallySpeculatable`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `X` | memref of any type values
| `X_shape` | memref of any type values
| `Y` | memref of any type values

#### Results:

| Result | Description |
| :----: | ----------- |
| `none_val` | none type

### `aismem.Transpose` (::spade::AISMEMTransposeOp)

_AISMEM Transpose Operator_

The Transpose operator stores data from input in output, but with the new shape, respectively output_shape
Traits: `AlwaysSpeculatableImplTrait`

Interfaces: `ConditionallySpeculatable`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `X` | memref of any type values
| `X_shape` | memref of any type values
| `Perm` | memref of any type values
| `Y` | memref of any type values

#### Results:

| Result | Description |
| :----: | ----------- |
| `none_val` | none type

