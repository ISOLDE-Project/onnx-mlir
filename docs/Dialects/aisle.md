<!-- Autogenerated by mlir-tblgen; don't manually edit -->
### `aisle.Add` (spade::AISLEAddOp)

_AISLE Add operation_

Performs element-wise binary addition (no support for Numpy-style broadcasting support).

Traits: `AlwaysSpeculatableImplTrait`

Interfaces: `ConditionallySpeculatable`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `A` | tensor of 16-bit float values or tensor of 32-bit float values or tensor of 64-bit float values
| `A_shape` | tensor of 32-bit signless integer values
| `B` | tensor of 16-bit float values or tensor of 32-bit float values or tensor of 64-bit float values
| `B_shape` | tensor of 32-bit signless integer values

#### Results:

| Result | Description |
| :----: | ----------- |
| `C` | tensor of 16-bit float values or tensor of 32-bit float values or tensor of 64-bit float values

### `aisle.Conv` (spade::AISLEConvOp)

_AISLE Conv operation_

The convolution operator consumes an input tensor and a filter, and
computes the output.

Traits: `AlwaysSpeculatableImplTrait`

Interfaces: `ConditionallySpeculatable`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `X` | tensor of 16-bit float values or tensor of 32-bit float values or tensor of 64-bit float values
| `X_shape` | tensor of 32-bit signless integer values
| `W` | tensor of 16-bit float values or tensor of 32-bit float values or tensor of 64-bit float values
| `W_shape` | tensor of 32-bit signless integer values
| `B` | tensor of 16-bit float values or tensor of 32-bit float values or tensor of 64-bit float values or none type
| `padding` | tensor of 32-bit signless integer values
| `stride_dilation` | tensor of 32-bit signless integer values

#### Results:

| Result | Description |
| :----: | ----------- |
| `Y` | tensor of 16-bit float values or tensor of 32-bit float values or tensor of 64-bit float values

### `aisle.GEMM` (spade::AISLEGEMMOp)

_AISLE GEMM Operator_

General Matrix multiplication: https://en.wikipedia.org/wiki/Basic_Linear_Algebra_Subprograms#Level_3
with the following hardcoded attributes
alpha = 1.0, betha = 1.0
transA = transB = 0 , i.e. no transpose

Traits: `AlwaysSpeculatableImplTrait`

Interfaces: `ConditionallySpeculatable`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>transA</code></td><td>::mlir::IntegerAttr</td><td>64-bit signed integer attribute</td></tr>
<tr><td><code>transB</code></td><td>::mlir::IntegerAttr</td><td>64-bit signed integer attribute</td></tr>
</table>

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `A` | tensor of 16-bit float values or tensor of 32-bit float values or tensor of 64-bit float values
| `A_shape` | tensor of 32-bit signless integer values
| `B` | tensor of 16-bit float values or tensor of 32-bit float values or tensor of 64-bit float values
| `B_shape` | tensor of 32-bit signless integer values
| `C` | tensor of 16-bit float values or tensor of 32-bit float values or tensor of 64-bit float values

#### Results:

| Result | Description |
| :----: | ----------- |
| `Y` | tensor of 16-bit float values or tensor of 32-bit float values or tensor of 64-bit float values

### `aisle.MatMul` (spade::AISLEMatMulOp)

_AISLE MatMul Operator_

Matrix product that behaves like numpy.matmul: https://docs.scipy.org/doc/numpy-1.13.0/reference/generated/numpy.matmul.html

Traits: `AlwaysSpeculatableImplTrait`

Interfaces: `ConditionallySpeculatable`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `A` | tensor of 16-bit float values or tensor of 32-bit float values or tensor of 64-bit float values
| `A_shape` | tensor of 32-bit signless integer values
| `B` | tensor of 16-bit float values or tensor of 32-bit float values or tensor of 64-bit float values
| `B_shape` | tensor of 32-bit signless integer values

#### Results:

| Result | Description |
| :----: | ----------- |
| `Y` | tensor of 16-bit float values or tensor of 32-bit float values or tensor of 64-bit float values

### `aisle.MaxPool` (spade::AISLEMaxPoolOp)

_AISLE MaxPool operation_

Max pooling consisting of computing the max on all values of a subset of the input tensor according to the kernel size and downsampling the data into the output tensor Y for further processing.

Traits: `AlwaysSpeculatableImplTrait`

Interfaces: `ConditionallySpeculatable`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `X` | tensor of 16-bit float values or tensor of 32-bit float values or tensor of 64-bit float values
| `X_shape` | tensor of 32-bit signless integer values
| `KernelStrides` | tensor of 32-bit signless integer values
| `Padding` | tensor of 32-bit signless integer values

#### Results:

| Result | Description |
| :----: | ----------- |
| `Y` | tensor of 16-bit float values or tensor of 32-bit float values or tensor of 64-bit float values

### `aisle.qconstant` (spade::AISLEQConstantOp)

_AISLE constant quadword operation_

Operation for holding constant data values. A quadword constant can have a
meaningful name recorded as its `name` attribute. Its content is stored
in the `value` dense/opaque element attribute.

Traits: `AlwaysSpeculatableImplTrait`

Interfaces: `ConditionallySpeculatable`, `NoMemoryEffect (MemoryEffectOpInterface)`, `mlir::ResultTypeInferenceOpInterface`

Effects: `MemoryEffects::Effect{}`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>shape</code></td><td>::mlir::Attribute</td><td>any attribute</td></tr>
<tr><td><code>name</code></td><td>::mlir::StringAttr</td><td>string attribute</td></tr>
<tr><td><code>value</code></td><td>::mlir::Attribute</td><td>any attribute</td></tr>
</table>

#### Results:

| Result | Description |
| :----: | ----------- |
| `output` | tensor of 8-bit unsigned integer values or tensor of 16-bit unsigned integer values or tensor of 32-bit unsigned integer values or tensor of 64-bit unsigned integer values or tensor of 8-bit signless integer values or tensor of 16-bit signless integer values or tensor of 32-bit signless integer values or tensor of 64-bit signless integer values or tensor of bfloat16 type values or tensor of 16-bit float values or tensor of 32-bit float values or tensor of 64-bit float values

### `aisle.ReduceMean` (spade::AISLEReduceMeanOp)

_AISLE ReduceMean operation_

Computes the mean of the input tensor's element along the provided axes.

Traits: `AlwaysSpeculatableImplTrait`

Interfaces: `ConditionallySpeculatable`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `X` | tensor of 16-bit float values or tensor of 32-bit float values or tensor of 64-bit float values
| `X_shape` | tensor of 32-bit signless integer values
| `Axes` | tensor of 32-bit signless integer values

#### Results:

| Result | Description |
| :----: | ----------- |
| `Y` | tensor of 16-bit float values or tensor of 32-bit float values or tensor of 64-bit float values

### `aisle.Relu` (spade::AISLEReluOp)

_AISLE Relu operation_

Relu takes one input data (Tensor) and produces one output data (Tensor) where 
the rectified linear function, y = max(0, x), is applied to the tensor elementwise.

Traits: `AlwaysSpeculatableImplTrait`

Interfaces: `ConditionallySpeculatable`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `X` | tensor of 16-bit float values or tensor of 32-bit float values or tensor of 64-bit float values
| `X_shape` | tensor of 32-bit signless integer values

#### Results:

| Result | Description |
| :----: | ----------- |
| `Y` | tensor of 16-bit float values or tensor of 32-bit float values or tensor of 64-bit float values

### `aisle.Reshape` (spade::AISLEReshapeOp)

_AISLE Reshape Operator_

Reshape the input tensor similar to numpy.reshape. 
First input is the data tensor, second input is a shape tensor which specifies the output shape. 
It outputs the reshaped tensor.

Traits: `AlwaysSpeculatableImplTrait`

Interfaces: `ConditionallySpeculatable`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `X` | tensor of 16-bit float values or tensor of 32-bit float values or tensor of 64-bit float values
| `X_shape` | tensor of 32-bit signless integer values
| `Y_shape` | tensor of 32-bit signless integer values

#### Results:

| Result | Description |
| :----: | ----------- |
| `Y` | tensor of 16-bit float values or tensor of 32-bit float values or tensor of 64-bit float values

### `aisle.Softmax` (spade::AISLESoftmaxOp)

_AISLE Softmax Operator_

The operator computes the normalized exponential values for the given input:

Traits: `AlwaysSpeculatableImplTrait`

Interfaces: `ConditionallySpeculatable`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `X` | tensor of 16-bit float values or tensor of 32-bit float values or tensor of 64-bit float values
| `X_shape` | tensor of 32-bit signless integer values

#### Results:

| Result | Description |
| :----: | ----------- |
| `Y` | tensor of 16-bit float values or tensor of 32-bit float values or tensor of 64-bit float values

### `aisle.Transpose` (spade::AISLETransposeOp)

_AISLE Transpose Operator_

The Transpose operator stores data from input in output, but with the new shape, respectively output_shape
Traits: `AlwaysSpeculatableImplTrait`

Interfaces: `ConditionallySpeculatable`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `X` | tensor of 16-bit float values or tensor of 32-bit float values or tensor of 64-bit float values
| `X_shape` | tensor of 32-bit signless integer values
| `Perm` | tensor of 32-bit signless integer values

#### Results:

| Result | Description |
| :----: | ----------- |
| `Y` | tensor of 16-bit float values or tensor of 32-bit float values or tensor of 64-bit float values

### `aisle.hstack` (spade::AISLEhstack)

_AISLE hstack operation_

Stack arrays in sequence horizontally (column wise). Functional equivalent to numpy.hstack, see https://numpy.org/doc/stable/reference/generated/numpy.hstack.html

Traits: `AlwaysSpeculatableImplTrait`

Interfaces: `ConditionallySpeculatable`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `A` | tensor of 16-bit float values or tensor of 32-bit float values or tensor of 64-bit float values
| `B` | tensor of 16-bit float values or tensor of 32-bit float values or tensor of 64-bit float values

#### Results:

| Result | Description |
| :----: | ----------- |
| `Y` | tensor of 16-bit float values or tensor of 32-bit float values or tensor of 64-bit float values

